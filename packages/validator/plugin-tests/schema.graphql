type User {
  id: ID!
}

type Post {
  title: String
}

input PostInput {
  id: ID!
  title: String!
  content: Int
  type: PostType
}

enum PostType {
  POST
  COMMENT
  BLOG
}

union Content = User | Post
